# =============================================================================
# JobHunterX — Python Requirements
# Optimized & Conflict-Free Installation
# =============================================================================

# Prefer prebuilt wheels over source builds. Some packages (e.g. litellm)
# publish sdists that require Rust/Cargo to compile; this flag makes pip pick
# the newest version that ships a binary wheel instead.
--prefer-binary

# --- Core Web Framework & Server ---
fastapi>=0.140.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
httpx>=0.27.0

# --- Data Schemas & Configuration ---
pydantic>=2.11.0
pydantic-settings>=2.8.0
python-dotenv>=1.1.0
langgraph>=1.2.9

# --- LLM Providers & Router ---
# litellm pinned to 1.80.0: browser-use pins openai==2.16.0 while litellm
# >=1.85.0 needs openai>=2.20 (unresolvable). Also ships prebuilt wheels,
# avoiding Rust toolchain builds.
litellm==1.80.0
google-genai>=1.65.0

# --- Search & Web Scraping ---
ddgs>=9.0.0
curl-cffi>=0.15.0
trafilatura>=2.0.0
beautifulsoup4>=4.13.0
lxml>=5.4.0
scrapegraphai>=1.0.0

# --- Document Processing & PDF Tailoring ---
xhtml2pdf>=0.2.0
jinja2>=3.1.0
pymupdf>=1.25.0

# --- Persistence & Caching ---
aiosqlite>=0.22.0
diskcache>=5.6.0
tenacity>=9.1.0

# --- System & Observability ---
structlog>=26.1.0
psutil>=7.0.0

# --- Browser Automation ---
browser-use>=0.13.0

# --- Testing & Development ---
pytest>=8.0.0
pytest-asyncio>=0.25.0
